<%- include("./partials/header"); -%>
    <h1>Home</h1>
    <p><%= HomeContent %></p>

 <!-- <%   for(var i=0;i< PostItems.length; i++){ %> 
        <h1><%= PostItems[i].PostTitle %></h1>
        <p><%= PostItems[i].PostBody %></p>
  <%  }   %> -->
 
    <% PostItems.forEach(function(element){ %>
        <h1><%= element.PostTitle %></h1>         
        <p><%=  _.truncate(element.PostBody, {'length':100, 'separator':" "}); %> <a href="/posts/<%= element.PostTitle %>">Read More</a></p>
    <% }); %>

<%- include("./partials/footer"); -%>